import { CpfSeniorPipe } from './cpf-senior.pipe';

describe('CpfSeniorPipe', () => {
  it('create an instance', () => {
    const pipe = new CpfSeniorPipe();
    expect(pipe).toBeTruthy();
  });
});

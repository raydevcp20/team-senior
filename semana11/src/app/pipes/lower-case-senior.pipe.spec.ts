import { LowerCaseSeniorPipe } from './lower-case-senior.pipe';

describe('LowerCaseSeniorPipe', () => {
  it('create an instance', () => {
    const pipe = new LowerCaseSeniorPipe();
    expect(pipe).toBeTruthy();
  });
});

import { UppercaseSeniorPipe } from './uppercase-senior.pipe';

describe('UppercaseSeniorPipe', () => {
  it('create an instance', () => {
    const pipe = new UppercaseSeniorPipe();
    expect(pipe).toBeTruthy();
  });
});
